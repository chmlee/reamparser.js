const { mutateString } = require('./mutateString.js');
const { mutateNumber } = require('./mutateNumber.js');
// const { mutateBoolean } = requrie('./mutateBoolean.js');

module.exports = {
  mutateString,
  mutateNumber,
};
